# Standalone crate: Zed compiles it to wasm32-wasip1 when the extension is
# installed, so it deliberately sits outside the candela Cargo workspace.
[workspace]

[package]
name = "zed_candela"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0"

[lib]
path = "src/candela.rs"
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"
