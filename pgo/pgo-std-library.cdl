fn main() {
    let count = 0;
    for _ in 0..100 {
        let s = "  Hello, World!  ";
        let s1 = s[0..2];
        let s2 = s[0];
        let r = range(1,5);
        let r2 = range(5);
        let t = s.trim();
        let tl = s.trim_left();
        let tr = s.trim_right();
        let ts = "-Hello-".trim_sequence("-");
        let tsl = "-Hello-".trim_sequence_left("-");
        let tsr = "-Hello-".trim_sequence_right("-");
        let u = t.uppercase();
        let l = u.lowercase();
        let c = t.contains("World");
        let f = t.find("World");
        let sw = t.starts_with("Hello");
        let ew = t.ends_with("!");
        let isf = "3.14".is_float();
        let isi = "42".is_int();
        let parts = l.split(", ");
        let joined = parts.join("-");
        let r = joined.replace("-", " ");
        let length = r.len();
        let rev = r.reverse();
        let rep = "ab".repeat(3);
        let n = 42.7;
        let sq = n.sqrt();
        let fl = n.floor();
        let ro = n.round();
        let ab = (-5).abs();
        let fab = (-3.14).abs();
        let to_f = float(42);
        let to_i = int(3.14);
        let to_s = str(42);
        let to_b = bool("true");
        let rng = range(10);
        let arr = [3, 1, 4, 1, 5];
        arr.sort();
        arr.reverse();
        arr.push(5);
        arr.remove(0);
        let arr2 = arr[0..2];
        let arr3 = arr[0];
        let arr4 = arr.find(1);
        print(arr.len());
        print(arr.contains(7));
        print(arr.contains(1));
        print(arr.repeat(2));
        print(arr.partition(4));
        fs::read("pgo/pgo-std-library.cdl");
        print(fs::exists("pgo/pgo-std-library.cdl"));
        print(argv());
        let map = {"test": 32, "othertest": 42};
        print(map.get("test"));
        map.insert("test", 42);
        map.insert("test2", 42);
        count += length;
    }
    print(count);
}